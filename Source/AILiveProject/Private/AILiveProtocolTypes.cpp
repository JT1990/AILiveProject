#include "AILiveProtocolTypes.h"

// Reflection-only translation unit; the generated.cpp emitted by UHT is
// compiled alongside this and provides all reflection hooks. Wire-string
// conversions for the four UENUMs live in AILiveProtocolJson.cpp where the
// JSON pipeline can use them directly.
